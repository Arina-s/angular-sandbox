import { Routes } from '@angular/router';
import { TabComponents } from './tabs/tab-components/tab-components';

export const routes: Routes = [
  {
    path: 'components',
    component: TabComponents
  },
];
